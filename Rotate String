Example 1:

Input: s = "abcde", goal = "cdeab"
Output: true
Example 2:

Input: s = "abcde", goal = "abced"
Output: false


Explanation:
make 2 lists for the given strings
iterate through the first string and pop the first element
add the popped one to the same string
next compare both the strings

code:
class Solution(object):
    def rotateString(self, s, goal):
        length = len(s)
        s = list(s)
        goal = list(goal)
        for i in range(length):
            element = s.pop(0)
            s.append(element)

            if s == goal:
                return True
        return False
        
